// Write a function that greets a user with a message. If no name is provided, it should use "Guest" as the default name.

function greet(name='Guest'){

    console.log("Welcom back",name);
};

greet();
greet('shafikul')